// Meeting Rooms II
// LeetCode (Medium)
// Amazon, Facebook

const minMeetingRooms = intervals => {
  if (!intervals || intervals.length === 0) {
    return 0;
  }
  intervals.sort((a, b) => a[0] - b[0]);

  // TODO
};
